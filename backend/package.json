{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "encore gen client --target leap && cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "encore.dev": "^1.48.8",
    "uuid": "^9.0.1",
    "googleapis": "^130.0.0",
    "@google/generative-ai": "^0.21.0",
    "@pinecone-database/pinecone": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
