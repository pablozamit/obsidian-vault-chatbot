{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'No install needed at root'",
  "functions": {
    "api/proxy/[...path].js": {
      "maxDuration": 30
    }
  }
}
